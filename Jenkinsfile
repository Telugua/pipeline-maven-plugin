#!/usr/bin/groovy

pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        echo 'myproject'
      }
    }
  }
}


/* pipeline {
          stages {
                    stage('Build') {
                              steps {
                                        dir ('myproject') {
                                                  sh 'mvn clean install'
                                        }
                              }
                    }
          }
} */
